import React from 'react';
import { makeStyles, Container, CssBaseline, Typography, Grid, TextField } from "@material-ui/core";

const HeroNavBar = () => {
    return (
        <Container maxWidth="sm">
            {/* <CssBaseline/> */}
            container
        </Container>
    );
};

export default HeroNavBar;